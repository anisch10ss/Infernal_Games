<form method="post" action="{{ path('blogs_delete', {'id': blog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this blog?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id) }}">
    <button class="btn">Delete</button>
</form>